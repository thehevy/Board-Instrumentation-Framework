# Development dependencies (not required for runtime)
pytest>=7.0.0
pytest-cov>=4.0.0

# Note: pytest.ini enables --cov by default, which requires pytest-cov above

# Note: Core library uses only Python stdlib
# No runtime dependencies required
